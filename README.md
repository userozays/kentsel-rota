# kentsel-rota

Bu branch boşaltıldı. Güncel çalışma **[`dev`](../../tree/dev)** branch'indedir.

## Geçmiş

`main` üzerindeki önceki proje (bina, malik, müteahhit, fizibilite, teklif ve
CSV dışa aktarma modülleri) silinmedi — commit geçmişinde duruyor:

```bash
git checkout f843335        # eski sürümün tamamı
git checkout f843335 -- src # yalnızca belirli bir klasörü geri almak için
```

Doğrudan tarayıcıdan görmek için:
<https://github.com/userozays/kentsel-rota/tree/f843335>
